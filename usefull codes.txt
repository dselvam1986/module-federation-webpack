"scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    
  }

  // angular remote
  "scripts": {
    "ng": "ng",
    "start": "ng serve --webpack-config webpack.config.js",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  